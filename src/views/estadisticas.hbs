



<div class="container row">
  {{#each mostrarcandidatos}}
    <div class="col-6 text-center card">
      <div class="card-img-top"></div>
      <img src="{{img}}" alt="" />
      <div class="card-body">
        <h1> {{nombrePartido}} </h1>
        <h2>{{votos}}</h2>
    <h1>{{porcentajevotos}}</h1>

      </div>
    </div>
  {{/each}}
</div>